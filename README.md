# caddy-cloudflare

Caddy with [caddy-dns/cloudflare](https://github.com/caddy-dns/cloudflare) module.

Built automatically on push and daily at 06:00 UTC via GitHub Actions.
Published to `ghcr.io/<owner>/caddy:latest`.
